node_modules/
__pycache__/
*.py[cod]
target/
libexec/attention-rs

# Local agent sessions, provenance, inboxes, and research runs.
.claude/
.inbox/
.prov/
.research/

# Planning write-ups are local working notes, not shipped docs.
docs/plans/
# Review write-ups are the same, except the handful already tracked stay
# tracked: git ignores nothing it is already following.
docs/reviews/*
