node_modules/
__pycache__/
*.py[cod]
target/
libexec/attention-rs

# Local agent sessions, provenance, inboxes, and research runs.
.claude/
.inbox/
.prov/
.research/

# Planning and review write-ups are working notes, not shipped docs. This only
# stops new ones being added; the handful already tracked stay tracked, because
# git ignores nothing it is already following.
docs/plans/*
docs/reviews/*
